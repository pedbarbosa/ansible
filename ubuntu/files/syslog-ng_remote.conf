# File managed by Ansible - https://github.com/pedbarbosa/ansible

# Syslog remote

source s_network {
	syslog(transport(udp) port(514));
};

destination d_remote {
	file("/var/log/remote_${HOST}.log");
};

log {
	source(s_network);
	destination(d_remote);
};

